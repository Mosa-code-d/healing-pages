# macOS system files
.DS_Store

# Windows system files
Thumbs.db

# Node modules (if you installed anything via npm)
node_modules/

# Environment variable files (if you add them later)
.env
.env.*.local

# Vite or other build tools output
dist/
build/
